vlans:
{% for region, data in REGIONS.items() %}
  {% for site in data.SITES %}
    - "!create_or_update:vid": 3001
      "!create_or_update:name": "{{ region }}-{{ site.name }}-VLAN3001"
      locations: ["!ref:{{ region }}-{{ site.name }}"]
      description: "VLAN 3001 - Management"
      status__name: "Active"
      "!ref": "{{ region }}-{{ site.name }}-VLAN3001"
    - "!create_or_update:vid": 3002
      "!create_or_update:name": "{{ region }}-{{ site.name }}-VLAN3002"
      locations: ["!ref:{{ region }}-{{ site.name }}"]
      description: "VLAN 3002 - Data"
      status__name: "Active"
      "!ref": "{{ region }}-{{ site.name }}-VLAN3002"
  {% endfor %}
{% endfor %}

